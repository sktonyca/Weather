import tkinter
from tkinter import *
import requests
from bs4 import BeautifulSoup

# Scraping Data
#URL = "https://weather.gc.ca/city/pages/on-118_metric_e.html"
#page = requests.get(URL)
#soup = BeautifulSoup(page.content, 'html.parser')
#temp = soup.find("span",class_="wxo-metric-hide")
#temp = temp.get_text()

# Create a window with 312x324
#window = tkinter.Tk()
# window.geometry("312x324")
#window.title("Weather App")
#intro = tkinter.Label(window, text="Welcome to the weather app").pack()
# print(temp)
#tempLabel = tkinter.Label(window, text=temp).pack()


# window.mainloop()
